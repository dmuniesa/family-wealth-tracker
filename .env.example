# Family Wealth Tracker - Environment Variables Template
# Copy this file to .env.local for development or .env for production

# Database Configuration
DATABASE_PATH=./data/wealth_tracker.db

# Encryption Key for IBAN encryption (32 characters minimum)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your-32-char-encryption-key-here-replace-this

# Session Secret for authentication (32 characters minimum)
# Generate with: openssl rand -hex 32
SESSION_SECRET=your-32-char-session-secret-here-replace-this

# Application Environment
NODE_ENV=production

# Optional: Custom port (default: 3000)
# PORT=3000

# Optional: Custom hostname (default: 0.0.0.0 for Docker)
# HOSTNAME=0.0.0.0

# Production Security Settings
# Set to true in production for secure cookies
# SECURE_COOKIES=true

# Cloud Storage Configuration (Optional)
# Dropbox OAuth - Create app at https://www.dropbox.com/developers/apps
# DROPBOX_CLIENT_ID=your-dropbox-client-id
# DROPBOX_CLIENT_SECRET=your-dropbox-client-secret

# Google Drive OAuth - Create project at https://console.developers.google.com
# GOOGLE_DRIVE_CLIENT_ID=your-google-drive-client-id
# GOOGLE_DRIVE_CLIENT_SECRET=your-google-drive-client-secret

# Instructions:
# 1. Copy this file to .env.local (development) or .env (production)
# 2. Replace all placeholder values with actual secure values
# 3. Never commit the actual .env file to version control
# 4. For Docker deployment, these variables are passed through docker-compose.yml